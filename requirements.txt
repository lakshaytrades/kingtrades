# ============================================================
# KingTrades — US Momentum Bot (Alpaca)
# Install: pip install -r requirements.txt
# ============================================================

# ---- Alpaca Broker SDK (US stocks + options, 4x leverage) ----
alpaca-py>=0.40.0             # Official Alpaca Python SDK — stocks, options, Greeks

# ---- Data & Analysis ----
pandas>=2.0.0
numpy>=1.26.0
pandas-ta>=0.3.14b            # Technical indicators (RSI, MACD, ATR, VWAP, etc.)
scipy>=1.10.0

# ---- Visualization ----
matplotlib
mplfinance>=0.12.10b0         # Candlestick charts
Pillow>=10.0.0

# ---- Telegram Notifications ----
python-telegram-bot>=20.0

# ---- HTTP & Requests ----
requests>=2.31.0
httpx>=0.25.0
aiohttp>=3.9.0
websockets>=12.0
tenacity>=8.2.0               # Retry logic with exponential backoff

# ---- News & Sentiment ----
newsapi-python>=0.2.7
feedparser>=6.0.10            # RSS feeds for economic calendar
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ---- Machine Learning (Trainer module) ----
scikit-learn>=1.3.0
optuna>=3.4.0                 # Hyperparameter optimization
joblib>=1.3.0

# ---- Async & Scheduling ----
schedule>=1.2.0
asyncio-throttle>=1.0.2
apscheduler>=3.10.0
APScheduler>=3.10.0

# ---- Environment & Config ----
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ---- Logging ----
loguru>=0.7.0
rich>=13.0.0

# ---- Security ----
cryptography>=41.0.0

# ---- Database (trade journal) ----
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ---- AI Brain ----
anthropic>=0.40.0             # Claude API — set ANTHROPIC_API_KEY in .env
google-genai>=1.0.0           # Gemini AI brain for market intelligence

# ---- Global Market Data ----
yfinance>=0.2.40              # Free global market data: VIX, FX, commodities
pandas-datareader>=0.10.0     # Economic data (FRED, World Bank)
ta>=0.11.0                    # Alternative TA library fallback
