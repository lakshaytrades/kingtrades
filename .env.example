# ============================================================
# NSE Momentum Groww AI Bot - Environment Variables
# Copy this to .env and fill in your actual values
# NEVER commit .env to git — it contains live trading credentials
# ============================================================

# ---- Groww API Credentials — SET ONCE, NEVER CHANGE ----
# Step 1: Run this on your VPS to get your vendor key:
#   cd /opt/kingtrades && python3 auth_groww.py --extract-key
# Step 2: Paste the output below. That's it — fully automatic forever.
GROWW_VENDOR_KEY=your_permanent_vendor_integration_key_here
GROWW_TOTP_SECRET=your_totp_base32_secret_from_groww_here

# ---- Optional (only needed for vendor key extraction, first time) ----
# Copy from groww.in → API keys → Access Token (only needed once to extract vendor key)
# You can delete this line after running python3 auth_groww.py --extract-key
# GROWW_AUTH_TOKEN=your_current_access_token_here

# ---- Telegram Bot ----
# Create bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# ---- Gemini AI Brain (primary AI — market intelligence, signal scoring, daily review) ----
# Get free key at: https://aistudio.google.com/app/apikey
# Required for AI-powered signal explanations, morning brief, and trade review
GEMINI_API_KEY=your_gemini_api_key_here
# Model: gemini-1.5-flash (fast, free tier) or gemini-1.5-pro (more accurate)
GEMINI_MODEL=gemini-1.5-flash

# ---- News API (for sentiment filter) ----
# Get free key at: https://newsapi.org
NEWS_API_KEY=your_newsapi_key_here
# Optional: Alpha Vantage for economic calendar
ALPHA_VANTAGE_KEY=your_alphavantage_key_here

# ---- Trading Configuration ----
# DANGER: Set to True ONLY when ready for LIVE real-money trading
LIVE_TRADING_ENABLED=False
# Maximum capital to deploy per day (INR)
MAX_DAILY_CAPITAL=50000
# Maximum risk per trade as % of capital
MAX_RISK_PER_TRADE_PCT=0.5
# Daily loss limit as % of capital
DAILY_LOSS_LIMIT_PCT=2.0

# ---- Watchlist (comma-separated NSE symbols) ----
# Leave empty to use auto-detected top liquid stocks
CUSTOM_WATCHLIST=RELIANCE,TCS,INFY,HDFCBANK,ICICIBANK,SBIN,BHARTIARTL,ITC,KOTAKBANK,LT

# ---- Option Chain Analysis ----
# Uses NSE public API — NO API key required
# Set to True to enable PCR, Max Pain, OI wall analysis
OPTION_CHAIN_ENABLED=True

# ---- FII/DII Flow Tracker ----
# Uses NSE provisional data — NO API key required
# Institutional flow tracking: FII buying = bullish tailwind
FII_DII_ENABLED=True

# ---- Volume Profile ----
# Computed from live Groww candle data — no external API needed
VP_ENABLED=True

# ---- Logging ----
LOG_LEVEL=INFO
LOG_DIR=logs

# ---- Timezone (ALWAYS IST — do not change) ----
TIMEZONE=Asia/Kolkata
